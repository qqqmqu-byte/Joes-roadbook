# 南疆路书 · 构建过程与功能设计说明

> 一份给「后来者 / 协作者 / 开源读者」看的开发记录：这项目是什么、怎么一步步长成现在这样、以及每个关键决定背后的「为什么」。
> 配套主程序：`index.html`（单文件应用）＋ 数据层 `data.js`。公网演示：**https://a9a858d4747e55325.bj8.agentos-app.net**

---

## 一、项目定位

一个**单文件、零构建、双击即开**的移动端「南疆 10 日自驾路书」Web App。灵感对标 Airbnb / Linear / Apple 的视觉语言，专为「在路上」的场景设计。

它要解决的核心问题：一趟真实的新疆自驾，信息散落在备忘录、截图、订票 App 里，路上查起来很碎。把这个路书做成**一个打开就能用的网页**——倒计时、每日行程、地图导航、餐厅、预算、安全清单全在一个口袋里。

| 维度 | 选择 | 说明 |
|------|------|------|
| 形态 | 单文件 HTML ＋ 外部数据层 | 一个 `index.html` 装下全部界面与逻辑；数据抽到 `data.js`，零依赖、零构建 |
| 目标设备 | 移动端优先 | 为 390px 视口设计，桌面端自动居中成手机样式 |
| 数据源 | 真实行程 | 酒店/租车/机票为作者真实花销；餐厅来自大众点评 + 84 篇小红书喀什美食笔记整理 |
| 部署 | 静态托管即可 | Vercel / Netlify / GitHub Pages / 本沙箱发布 / 任意静态服务均可 |

---

## 二、技术栈与形态决策（为什么是「单文件 HTML」）

这是整个项目最底层、也最反直觉的一个决定：**不拆分成 HTML/CSS/JS 三件套，不做任何打包**。

> **为什么不用 Vite + React / Vue？**
> 旅行类开源项目最大的传播阻力是「clone 下来还要 `npm install` + `npm run dev`」。而一个路书最迷人的地方是「别人 fork 改改数据就能用自己的」——单文件让这件事成本降到最低：复制一个 `.html`、改 `data.js` 即可。

| 技术 | 用途 | 为什么这样选 |
|------|------|-------------|
| **Tailwind CSS（CDN）** | 快速排版 | 开发期极致提速；生产环境仍是 CDN 引入，不进构建链 |
| **内联 fallback 样式** | 保底布局 | 关键布局用 CSS 变量 + 内联 style 兜底，CDN 挂了核心 UI 仍可用 |
| **Leaflet（CDN）** | 地图 | 轻量、CDN 友好，比高德/百度官方 JS API 更适配纯前端 |
| **CSS 自定义属性** | 设计令牌 | `terracotta` 陶土色系 + 三层阴影系统，统一视觉语言 |
| **原生 JS（无框架）** | 全部逻辑 | 状态用几个全局变量管理，避免框架体积，单文件更可控 |
| **PWA manifest（内联）** | 加到主屏幕 | 只注册 manifest，「添加到主屏幕」即可，不强行做 ServiceWorker 离线 |
| **数据层 `data.js`** | 内容与逻辑解耦 | 行程/坐标/餐厅/预算/装备/贴士全部外置；支持 `localStorage` 覆盖与 JSON 导入导出，真正实现「填数据即生成路书」 |

---

## 三、功能清单

### 3.1 仪表盘（首页）
| 功能 | 说明 |
|------|------|
| **出发倒计时** | 64px 大字号「距出发 N 天」，视觉冲击优先 |
| **今日卡片 / 进度轴** | 行程进行中显示「今天在哪、第几天」；进度轴串联 10 天 |
| **4 个快捷入口** | 直达行程 / 地图 / 美食 / 安全四个核心模块 |
| **行程概览** | 一次性列出全部 10 天（含每日强度、里程、住宿参考价） |
| **回到今天（浮窗）** | 浏览到任意一天后，浮动按钮一键跳回当前日 |

### 3.2 行程（行程 Tab）
- 每日时间轴：时间点 + 标签（必去 / 出片 / 美食 / 避坑 / 交通）
- 每个地点带「导航」按钮 → 唤起多地图选择
- 租车内嵌说明（两段分租策略、各天用车与还车）

### 3.3 地图（地图 Tab）
- 基于 Leaflet，**多图源兜底**：高德 → OSM → ArcGIS，任一瓦片源失败自动切换
- 全量 POI 标注 + 每日路线 polyline（不同天不同颜色）
- 筛选：总览 / 锦囊 / 按天

### 3.4 美食（美食 Tab）
- **23 家餐厅**，覆盖喀什 / 库车 / 阿克苏
- 每家可点开弹层：地址、介绍、特色菜
- **喀什单独做 3 大类 tab**：硬核主食 / 街头小吃 / 甜品饮品（依据 84 篇小红书笔记聚类）
- **吃货避坑指南**：本地人视角的小红书式提醒

### 3.5 安全（安全 Tab，原「贴士」）
- 边防证办理、高原反高反装备清单、紧急电话
- 帕米尔自驾注意（加油、信号、防滑链）
- 命名为「安全」并配警示图标，强化旅途安全心智

### 3.6 预算看板（弹层）
- **14 项真实花销**：住宿 / 机票 / 租车（拼车 ÷2）/ 各类杂费
- 进度条 + 已确认 / 总项动态统计

### 3.7 装备清单（弹层）
- **5 类高原自驾装备**：防高反 / 洋葱穿搭 / 证件 / 电子 / 车载

### 3.8 导航（多地图）
- 点「导航」弹出**高德 / 百度 / 腾讯 / Apple 地图**四选一
- 为什么做四选一：不同用户、不同手机预装习惯不同，且高德在国内 POI 最准、Apple 在 iPhone 体验最好——交给用户选最稳。

### 3.9 路书数据编辑器（⚙️）
- 右下角齿轮按钮唤起底部半屏
- **导出**当前路书为 `roadbook.json` · **导入** JSON 文件或粘贴文本 → 立即生成专属路书
- **恢复默认**一键回滚
- 导入数据存浏览器 `localStorage`，刷新不丢；导入时与默认数据做字段合并，缺字段自动回退默认

---

## 四、构建过程（迭代时间线）

这个项目不是一次成型的，而是沿用户的真实使用反馈逐轮打磨：

1. **数据校准** — 先核对每日酒店是否正确（喀什希x顿→喀什大学城希x顿惠庭；库车希x顿 2 晚而非 3 晚），把行程地基打牢。
2. **视觉升级** — 参照 Airbnb/Linear/Apple 重做视觉：倒计时、进度轴、浮动「回到今天」、tab 角标、标签对比度、时间轴样式。
3. **修 CSS 事故** — 一次编辑丢失了 `<style>` 开标签，全站样式失效；重写整段设计系统修复。
4. **导航真正可用** — 「导航」按钮原本只是摆设，改成多地图选择弹层，真正能拉起地图 App。
5. **清单取舍** — 加过「出发前清单」「酒店费用卡」，又按用户要求移除，保持界面聚焦。
6. **真实预算** — 用真实数据替换占位：酒店价、租车（坦克400/比亚迪元）、机票，拼车费用 ÷2。
7. **Bug 清扫** — 修滑动冲突（非行程页误触、tab 栏误触、D1 滑不回概览）、分页点、行程概览只显示 5 天（改为全部 10 天）。
8. **餐厅数据** — 依次补全库车、阿克苏（每家带弹层）、喀什（3 分类 + 避坑指南）。
9. **开源准备** — 脱敏（酒店品牌/价格/楼栋地址脱敏、无航班号仅留航线、移除腾讯 beacon 埋点）、补 README / LICENSE / 截图占位。
10. **数据层外置** — 将 `days` / `poiCoords` / `mapQueries` / `restaurants` / `travelTips` / `budget` / `gear` 抽到 `data.js`，新增 ⚙️ JSON 导入导出编辑器，实现「填数据即生成路书」。
11. **部署上线** — 通过「发布为应用」生成公网链接，任意设备浏览器可访问；同步推送到 GitHub。

---

## 五、关键设计决策与「为什么」

### 5.1 倒计时用 64px 大字号
> 用户原计划是 48px。我坚持 64px——首页是「第一眼」，倒计时是这趟旅行的情绪锚点，大字号带来的仪式感远超几像素的精致。

### 5.2 地图做多图源兜底（高德 / OSM / ArcGIS）
> 国内地图瓦片环境复杂：高德最稳但需注意坐标系，OSM 在国内常被墙，天地图要 key。**按「中国大陆可达性 + 稳定性」排序自动降级**，弱网/特殊网络下也不会白屏。

### 5.3 导航做四地图选择而非绑定一个
> 不替用户决定用哪个地图 App。高德 POI 最准、Apple 在 iPhone 体验最佳、百度/腾讯覆盖广——弹层四选一，谁装了点谁。

### 5.4 底部弹层（bottom-sheet）模式
> 导航、预算、餐厅详情、装备都用 iOS 风格的底部上滑半屏。移动端原生感强、单手可操作，比居中弹窗更适合路上场景。

### 5.5 喀什餐厅做三分类 tab
> 不是拍脑袋分，是**把 84 篇小红书喀什美食笔记按「吃什么」聚类**后得出的真实维度：硬核主食（抓饭/馕包肉）、街头小吃（烤包子/烤肉）、甜品饮品（冰淇淋/酸奶）。分类服务于「到了喀什该先吃哪类」。

### 5.6 脱敏策略：品牌/价格/地址脱敏 + 移除埋点 + 保留武汉
> 开源前必须处理隐私。**真正高危的是 MAC 地址、Tailscale IP（网络设备标识）——已确认从未进文件**。示例数据做了以下脱敏，且**刻意不保留任何可反查的个人组合**：
> - **酒店品牌脱敏**：知名连锁酒店品牌改为「首字保留 + 末字变 x」（如某尔顿 → 某x顿）
> - **价格脱敏**：所有个人花费保留前两位数字 + `xx`，仅留量级（如 `¥xxxx` 形式）
> - **具体楼栋地址脱敏**：精确到楼栋/门牌的地址改为只保留到道路（如「某路 113 号某大厦」→「某路」）
> - **无具体航班号**：仅保留航线（如武汉→喀什），不绑定航司班次，无法反查
> - **倒计时日期保留精确值**（驱动「距出发 N 天」功能所必需），但不与任何个人身份绑定
> - **保留「武汉」作为出发城市**（旅行博客常见，单独出现不具辨识度，且保留「真实路书」可信度）
> - **移除腾讯 beacon 埋点**（避免 fork 后把访客数据回传给原作者）

### 5.7 PWA：只做 manifest，不做 ServiceWorker
> 「添加到主屏幕」体验很重要，但单文件 PWA 不强求离线优先。内联 manifest 即可获得入口，避免 Blob ServiceWorker 的 MIME 坑。

### 5.8 内联兜底样式
> 关键布局用 CSS 变量 + 内联 style 写死，即便 Tailwind CDN 加载失败，核心 UI 仍可阅读使用。这是为「极端弱网/被墙」场景买的保险。

### 5.9 数据层外置（data.js + JSON 编辑器）
> 把「内容」与「程序」解耦：路书所有信息都在 `data.js`，页面只负责渲染。配合 ⚙️ 编辑器，用户导出 JSON 改一改再导入，就能生成自己的路书——这是项目从「南疆专属」升级为「可复用旅行产品」的关键一步。

---

## 六、踩过的坑与环境约束

| 问题 | 现象 | 处理 |
|------|------|------|
| `<style>` 标签丢失 | 一次编辑丢失开标签，全站样式失效 | 重写整段设计系统 |
| Tailwind CDN 在沙箱 SSL 失败 | 无头截图时 CDN 握手失败，只剩 inline 兜底样式 | 本地化 tailwind 截图验证，生产仍走 CDN |
| 滑动误触 | 非行程页也响应滑动、tab 栏被划动 | 加 `activeView` / `.no-scroll` 判断 |
| 行程概览只显示 5 天 | 分页 `Math.min(startIdx+5,...)` 写死 | 改为 `days.length` 显示全部 10 天 |
| Tailwind config 少一个 `}` | `window.tailwind.config` 语法错误，自定义配色从未生效 | 补全括号（4 个关闭括号），自定义品牌色板恢复生效 |
| **沙箱无法 git 推 GitHub** | git 链接 gnutls，被出口 TLS 拦截代理 reset；SSH 22 端口也被封 | 改为用户本机推（SSH 通）；沙箱改用「发布为应用」生成公网链接 |

> **关键约束**：本沙箱的 `github.com` 被解析到保留测试网段（`198.18.0.6`）走透明 TLS 拦截代理。curl / `gh`（Go TLS）能通，但 git 的 gnutls 握手被掐断——**因此从沙箱用 git 推 GitHub 在架构上不可行**，代码推送交由用户在本机完成。

---

## 七、文件结构

```
Joes-roadbook/
├── index.html                    # 主程序（单文件应用，界面 + 渲染逻辑，已脱敏、已移除埋点）
├── data.js                       # 数据层（days/poiCoords/mapQueries/restaurants/travelTips/budget/gear）
├── data.example.json             # 数据模板（由默认数据导出，照着填空即可）
├── README.md                     # 产品级项目介绍 + 上手/贡献指引
├── 构建过程与功能设计.md          # 本文档
├── LICENSE                       # MIT
├── .gitignore
└── screenshots/                  # （可选）README 用截图，自行补充
```

仓库分支为 `main`。

---

## 八、开源与部署状态

- **脱敏**：已完成（酒店品牌、价格、具体楼栋地址脱敏；无具体航班号，仅保留航线；腾讯 beacon 埋点已移除；MAC/Tailscale/IP 等网络设备标识从未入库；倒计时精确日期保留以驱动功能，不与个人身份绑定）。
- **数据层**：已外置为 `data.js`，支持 JSON 导入导出，详见 `README.md`。
- **GitHub**：仓库结构就绪，推送由用户在**本机**执行（沙箱 git 通道受限）：
  ```bash
  git remote add origin git@github.com:<用户名>/Joes-roadbook.git
  git push -u origin main
  ```
- **公网访问**（不依赖 GitHub，立即可用）：
  > **https://a9a858d4747e55325.bj8.agentos-app.net**

---

## 九、想贡献 / 改自己的路书？

本项目已做到**零构建、纯数据驱动**，改自己的路书有两种方式：

**方式 A：编辑 `data.js`（适合开发者）**
直接改 `data.js` 里的字段，无需任何构建：
- `tripStart` / `tripEnd` — 出发/返程时间（倒计时基准，ISO8601）
- `days` — 每日行程（时间轴、地点、标签、住宿、费用、贴士）
- `poiCoords` — 地图打点经纬度 `[lat, lng]`
- `mapQueries` — 导航唤起时的地图搜索词
- `restaurants` — 餐厅（城市 / 分类 / 地址 / 介绍 / 特色菜）
- `travelTips` — 安全/贴士分类内容
- `budget` — 预算看板（项目 / 金额 / 是否真实 / 备注）
- `gear` — 高原自驾装备清单（分类 / 说明）

改完双击 `index.html` 或本地起静态服务即可看效果。

**方式 B：⚙️ 内嵌编辑器（适合所有人，无需写代码）**
打开页面 → 点右下角齿轮 → 「⬇️ 导出 JSON」拿出模板 → 按结构改成你的行程 → 「⬆️ 导入文件」或粘贴 JSON → 「✓ 应用」，页面秒变你的专属路书。导入数据存浏览器本地，刷新不丢。

> 也可直接复制根目录 `data.example.json` 作为填空模板。

**方式 C：交给 Agent（grillme 法，推荐给不想碰 JSON 的人）**
你 clone 下整个仓库后，可以不自己填数据——把建路书的任务交给你的 AI Agent，让它用 **grillme 法**：主动一轮轮追问你的目的地、航班/高铁班次、每日节奏、住宿预算，以及你收藏的 **小红书 / 抖音 / 马蜂窝** 攻略链接或截图，把零散信息榨成结构化数据，再直接产出一份符合 `data.example.json` 结构的 `roadbook.json`（或 `data.js`）。

给 Agent 的 prompt（复制即用）：

```text
你是一个路书构建助手。我已 clone 了 Joes-roadbook（data.js + index.html）。
请用 grillme 法帮我构建我自己的自驾路书：
1. 逐个追问：目的地（城市/区县/具体景点）、出行日期与天数、出发城市与航班/高铁班次时刻、每日必去点/出片机位/美食清单/避坑、住宿与预算，以及我收藏的参考数据源（小红书/抖音/马蜂窝链接或截图）。
2. 收到后映射到 data.js 字段，或直接产出符合 data.example.json 的 roadbook.json。
3. 尽量读取我提供的攻略链接/截图补全 POI、餐厅、地图坐标。
4. 产出后给我本地预览步骤并支持迭代。
```

> 关键思路：**参考数据源（小红书/抖音）是路书质量的杠杆**——你喂给 Agent 的攻略越具体，产出的 POI、餐厅、坐标越准，路书越像「你真的去过」。

欢迎提交你的路书版本或新功能（如可视化表单编辑器、离线缓存、暗色模式、多路线切换）。
